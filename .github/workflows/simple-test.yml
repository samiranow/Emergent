name: Simple Test Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:  # برای اجرا به صورت دستی

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print hello world
        run: echo "Hello World"
